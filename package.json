{
  "name": "vite-react-app",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently \"npm run start:server\" \"npm run dev\"",
    "start:server": "npm run --prefix server start",
    "setup-and-dev": "node scripts/bootstrap.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^8.2.0"
  }
}
